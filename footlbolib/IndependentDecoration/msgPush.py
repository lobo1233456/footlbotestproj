import json
import requests


class msgPush():
    def urlBasefun(self,a,b):
        exist = False
        a,b = set(a),set(b)
        len(a&b)
        if len(a&b)==0:exist = True
        return exist
    def mailSend(self):
        pass

